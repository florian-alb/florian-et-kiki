{
	"devDependencies": {
		"husky": "^9.1.7"
	},
	"scripts": {
		"prepare": "husky",
		"test": "echo \"Error: no test specified\" && exit 1",
		"api-gateway": "cd api-gateway && pnpm run dev",
		"private-api": "cd private-api && pnpm run dev",
		"public-api": "cd public-api && pnpm run dev"
	}
}
